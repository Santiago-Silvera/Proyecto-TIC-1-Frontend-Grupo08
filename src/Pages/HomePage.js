import MovieList from "../Components/MovieList";
import Listings from "../Components/Listings";

const Home = () => {
  return (
    <div>
      <MovieList />
      <Listings />
    </div>
  );
};

export default Home;
